# API Configuration (optional)
# Default: https://beta.aimo.network for production, http://localhost:8000 for local
# API_BASE=https://beta.aimo.network

# API Domain for SIWx signing (optional)
# Override to use a different domain for client-side SIWx message signing
# Default: derived from API_BASE hostname
# API_DOMAIN=beta.aimo.network

# EVM Private Key (with 0x prefix)
# Example: 0x0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
EVM_PRIVATE_KEY=

# Solana Private Key (base58 encoded 64-byte secret key, i.e. private + public)
# Example: 4vMsoUT...
SOLANA_PRIVATE_KEY=
