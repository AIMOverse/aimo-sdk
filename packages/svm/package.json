{
  "name": "@aimo.network/svm",
  "version": "1.0.0",
  "description": "AiMo Network Solana signer utils",
  "keywords": [
    "sdk",
    "svm",
    "solana"
  ],
  "author": "Archer <archer@aimo.network>",
  "license": "ISC",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm run clean && pnpm run compile",
    "clean": "rimraf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@aimo.network/client": "workspace:*",
    "@solana/kit": "~5.1.0",
    "@x402/core": "~2.1.0",
    "@x402/svm": "~2.1.0",
    "bs58": "^6.0.0"
  },
  "devDependencies": {
    "rimraf": "~6.1.0",
    "typescript": "~5.9.0"
  }
}
